<%= question_form_header(@question.test, @method) %>
<%= simple_form_for [@test, @question] do |f| %>
  <%= f.label(:body, 'Body') %>:<br>
  <%= f.text_area(:body) %><br>
  <%= f.button :submit %>
<% end %>
